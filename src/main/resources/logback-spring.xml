<?xml version="1.0" encoding="UTF-8"?>
<!--
  Structured JSON logging using logstash-logback-encoder.

  PatternLayoutEncoder-based JSON breaks when log messages contain double-quotes,
  backslashes, newlines, or control characters (e.g. exception stack traces or
  URLs with query strings). LogstashEncoder correctly escapes all values.

  The traceId MDC key is promoted to a top-level JSON field via <includeMdcKeyName>
  so CloudWatch Logs Insights can filter directly:
    fields traceId | filter traceId = "abc-123"
-->
<configuration>

  <springProfile name="!test">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <includeMdcKeyName>traceId</includeMdcKeyName>
        <!-- Remove the default "tags" array — unused and adds noise -->
        <excludeProvider class="net.logstash.logback.composite.loggingevent.TagsJsonProvider"/>
        <fieldNames>
          <timestamp>@timestamp</timestamp>
          <level>log.level</level>
          <logger>log.logger</logger>
          <thread>thread</thread>
          <message>message</message>
          <stackTrace>error.stack_trace</stackTrace>
        </fieldNames>
      </encoder>
    </appender>

    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="com.urlshortener" level="DEBUG"/>
  </springProfile>

  <!-- Plain text in tests for readable output -->
  <springProfile name="test">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
      </encoder>
    </appender>

    <root level="WARN">
      <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="com.urlshortener" level="INFO"/>
  </springProfile>

</configuration>
